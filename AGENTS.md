# 项目踩坑与教训 (AGENTS.md)

- [T-BE-002] 契约-实现漂移: OpenAPI contract 中 `RegisterRequest` 定义了 `verify_code` 为必填字段, 但 handler 的 `registerRequest` struct 和 service 的 `Register` 方法均未做任何校验。更新 contract 后必须逐层检查: handler struct → handler 调用 → service method 签名 → service 验证逻辑 → 测试覆盖。只改 YAML 不改代码等于没改。
- [T-BE-002] GET 接口可见性鉴权遗漏: `GET /rankings/{id}` 最初未校验非公开排行榜的访问权限, "私密" 排行可被任意用户读取。开发时容易形成 "只有写入才需鉴权" 的盲区。标准化做法: 所有资源 GET handler 都应从 context 提取 `userID` 并传给 service 层做可见性判断, 哪怕路由标记为 "公开"。
- [T-BE-002] handler request struct / service method 签名 / 测试三方不同步: 添加 `verify_code` 字段需要同时在 handler struct 加 field、handler 调用传参、service method 加参数。缺任何一环数据流就无声断掉。预防: contract schema 变更后, grep 搜索对应 handler 的 request struct 和 service method 签名确认都已更新。
- [T-BE-002] service 构造函数入参变更会波及所有调用者: `NewAuthService` 新增 `db *gorm.DB` 参数后, `router.go` 中所有调用处必须同步更新。这类 "构造链断裂" 容易在独立开发时被忽略, 需在变更构造函数后 `go build` 确认全链路通过。
- [T-FE-003] axios DELETE 请求体传参的隐式语法: `http.delete(url, { data: body })` 而非 `http.post(url, body)`, 因为 axios 的 `delete` 方法签名是 `(url, config?)` 而非 `(url, data, config?)`。所有需要发送请求体的 DELETE 接口 (如注销账号) 都必须将 body 嵌套在 config 的 `data` 字段中, 否则 body 会被忽略。新增 DELETE 带 body 接口时优先参考 `profileAPI.ts` 的 `deleteAccount` 写法。
- [T-FE-003] MSW handler 路径必须包含完整 `/api/v1` 前缀: MSW 拦截的是 axios 拼接 baseURL 后的**实际请求 URL**, 因此 handler 中路径必须写 `/api/v1/auth/send-verify-code` 而非 `/auth/send-verify-code`。未来新增 MSW mock 时, 如果在 handler 中只写相对路径, mock 不会触发且无任何报错提示, 调试成本极高。
- [T-FE-003] OpenAPI 中文枚举字面量的对齐风险: `VisibilityEnum` / `CategoryEnum` 使用中文字面量 (`'公开' | '私密' | '仅链接'` 等), 型别定义在 `types/common.ts`。修改这类枚举需要在 **4 处同步**对齐: OpenAPI YAML enum 列表 → `types/common.ts` 类型定义 → `utils/constants.ts` 运行时常量 → 后端实际存储的值。任何一处不一致不会产生编译错误, 只会导致运行时静默跳过匹配或逻辑分支失效。
- [T-FE-006] 无限滚动+筛选列表页存在两种不一致实现, 后续新增应统一使用 `useRef` 模式: DiscoverPage 用 `useRef` 缓存筛选值和分页偏移 (`filterRef` + `pageOffsetRef`), `loadMore` 回调依赖极简, 从根本上避免闭包陈旧; MyRankingsPage 用 `useCallback` + `reset=true` 参数 + `eslint-disable-next-line` 绕过陈旧 offset 闭包, 模式脆弱。后者若被删除 `reset` 参数或修改内部逻辑会无声出 bug。后续新增列表页 (分类浏览、搜索页等) 应统一使用 DiscoverPage 的 `useRef` 模式 + 复用 `useInView` hook。
- [T-FE-005] `useEffect` + async 的竞态处理 (`cancelled` flag 模式): 筛选列表页的关键词/分类变化会导致 effect 重新执行。若前一次 async 请求在 cleanup 后返回, `setState` 会覆盖已经更新的数据。必须在 effect 内使用 `let cancelled = false` + cleanup 中设置 `cancelled = true`, 在 async 回调的 try / catch / finally 三段都检查 `if (cancelled) return`。仅靠 `useRef` 解决闭包陈旧 (T-FE-006 教训) 不能防竞态 — 两者是正交问题, 需同时实现。参考 `DiscoverPage.tsx` 中 `loadInitial` 的实现。
- [T-FE-006] MSW mock 数据是模块级可变数组 handler 间共享: `mockRankings` 在 handlers.ts 模块作用域定义, `POST /rankings` handler 通过 `unshift` 追加, `DELETE /rankings/:id` handler 通过 `splice` 删除, 变更在同一 MSW 会话内跨请求持久。新增 MSW handler 时如读取或修改共享 mock 数组, 必须意识到这是可变共享状态, 不是每次请求重置的只读数据。
- [T-FE-009] MSW 共享 mock 对象被 `Object.assign` 原地修改后通过引用传播到所有 handler: `mockUser` 是模块级对象, 被 `mockRankings` 的 `user` 字段、auth 登录/注册响应、排行榜详情等多处 handler 通过 `user: mockUser` 引用嵌入。`PUT /api/v1/profile` handler 使用 `Object.assign(mockUser, updated)` 原地修改后, 所有引用了 `mockUser` 的 handler 响应都无声改变——更新昵称后, rankings 详情里嵌入的 user 对象也同步变了。预防: 共享 mock 对象应使用工厂函数（如 `() => ({...mockUser})`）按需生成副本, 或 handler 内局部 `{ ...mockUser, ...patch }` 返回新对象而非原地修改; 禁止在 MSW handler 中用 `Object.assign` 或直接属性赋值改动全局 mock 对象。
- [T-FE-008] 纯数据转换函数应在第二次出现时立即提取到 `src/utils/`: `groupEntriesByTier(entries: EntryResponse[])` 在 RankingDetailPage、SharePage、SharePosterModal、RankingManagePage 共 4 个文件中重复定义了几乎相同的分组+排序逻辑。这类函数无 hook/无渲染依赖, 纯输入输出, 第一次出现在某个文件时还可接受, 第二次出现就必须提取到 `src/utils/`。否则后续修改分组逻辑或新增 tier 等级时需改动 4 处, 极易遗漏。
- [T-FE-008] html2canvas 的 CORS 跨域请求静默失败: 开启 `useCORS: true` 后, 跨域图片加载失败不会抛出可捕获的异常, 只会渲染为空白区域且无任何 console 警告。检查 `html2canvas` 输出是否包含图片不能用 try/catch, 必须提前确保所有 `<img>` 的 src 是 same-origin 或目标服务器启用了 `Access-Control-Allow-Origin` 头。开发阶段使用 `picsum.photos` 等外部图床时, 海报中的图片必定空白。解决: 开发阶段用 `useCORS: false` 并在 localhost 直接加载同源 Mock 图片, 或切换为 data URI / base64 占位图。
- [T-FE-008] html2canvas 捕获目标必须可见地存在于 DOM 中: `display: none` 或 `visibility: hidden` 会导致 `html2canvas` 捕获结果为空白画布, 且不会报错。标准模式是将目标元素通过 `position: absolute; left: -9999px; top: 0; pointer-events: none;` 移出屏幕, 同时保持其在 DOM 树中可见和可渲染。使用 `position: fixed` 也可行但需注意 fixed 定位是相对于视口而非最近定位父元素。后续任何需要使用 `html2canvas` 截图的新组件都应直接复制此 off-screen 模式而非尝试 `display: none`。
- [T-FE-004] LoginPage 双模式(邮箱/用户名)对后端 account 字段是多态输入: LoginRequest 只有一个 `account` 字符串字段, 后端 service 层必须在 `FindByEmail` 失败后回退到 `FindByUsername` 双查找。这就要求 usernames 不能包含 `@` 字符(否则与邮箱格式歧义), 是一个设计层面的隐性约束, 新增账户类型或修改用户名规则时必须考虑。
- [T-FE-004] setInterval 倒计时必须配合 useEffect 卸载清理: RegisterPage 的 60s 倒计时用 `useRef<ReturnType<typeof setInterval>>` 存 interval ID 并在 `startCountdown` 中手动清理, 但缺少 `useEffect(() => () => clearInterval(ref.current), [])` 的组件卸载 cleanup。组件卸载后 interval 仍在触发 `setCountdown(prev => ...)` 造成无人响应的异步回调。写倒计时/轮询时先用 useEffect return 做 cleanup 再写业务逻辑, 不要依赖手动清理兜底。
- [T-FE-004] API 错误消息提取存在重复且脆弱的 inline 类型断言模式: LoginPage 和 RegisterPage 各自独立写了 `(err as { response: { data?: { message?: string } } }).response?.data?.message` 类型断言, 当后端响应格式变化时需要逐一修改所有页面。新页面遇到 API 调用错误处理时, 应优先提取共享工具函数(如 `src/utils/getErrorMessage.ts`)统一处理, 不要复制 inline 类型断言。
- [T-FE-007] @dnd-kit 跨容器拖拽必须用三阶段 handler 分解: `onDragStart` 设 `activeEntry` → DragOverlay 渲染, `onDragOver` 做实时跨容器移动 + 设 `overContainer` → 目标行高亮, `onDragEnd` 做行内排序 (`arrayMove`) + 设 `isDirty` + 触发防抖保存。关键约束: 跨容器 move 逻辑必须放在 `onDragOver` 而非 `onDragEnd`, 因为 `onDragOver` 在拖拽悬浮到新容器时持续触发并提供实时视觉反馈; 若只做在 `onDragEnd`, 拖拽全程无中间态, 用户看到的只是卡片在释放瞬间跳跃到新容器。行内排序 (`arrayMove`) 则必须在 `onDragEnd` 做, 因为 `onDragOver` 时同一容器内排序的 `activeIndex` / `overIndex` 会频繁变化引起不必要的 state 震荡。
- [T-FE-007] 防抖保存闭包陈旧的三行修复模式: 在 `useState` 定义后立即加 `const ref = useRef(items); ref.current = items` (render 体每轮同步), 然后在 `setTimeout` / async 回调中读 `ref.current` 而非闭包捕获的 state。这个模式与 T-FE-006 的 `useRef` 避开闭包不同: T-FE-006 是把值存在 ref 中从源头避免创建闭包; T-FE-007 是先有 state, 再通过 ref 镜像解决 debounced 回调中的陈旧引用。新增任何带防抖保存的页面 (如批量编辑、图片排序) 优先采用此模式, 否则 800ms 内多次操作可能导致保存的快照丢失中间变更。
- [T-FE-010] 测试文件重定义生产类型导致静默漂移: RankingManagePage.test.ts 中重新定义了 `EntryResponse` / `TierEnum` / `ItemsMap` / `ContainerKey` 等类型, 而非从 `@/types` 导入。生产代码修改类型后测试照常通过但语义不再匹配。新增测试文件时应优先 `import` 生产类型, 仅在有特殊 mock 需求时定义局部变体, 并在定义处加注释说明偏离原因。
- [T-FE-010] 测试方法名与断言逻辑矛盾: saveProtection.test.ts 中方法名 `'rejects empty updates array'` 但断言期望 `code=200` 通过。方法名是文档断言是证据, 两者矛盾让后续开发者误判行为语义。写测试时先写断言再反向拟名, 或在 review 中人工检查 name / assert 一致性。
- [T-FE-010] 交互测试只覆盖纯函数未覆盖真实 @dnd-kit DndContext 交互: RankingManagePage.test.ts 测试了 `groupEntriesByTier` / `buildReorderUpdates` 等工具函数, 但任务要求「跨行拖拽等级变更、行内排序、拖入待放置区、拖入删除区」需依赖 `DndContext` / `useDroppable` / `useSortable` 真实渲染。写交互测试时应核对任务需求中的交互动词(拖拽/拖入/排序), 确保每个交互动词对应至少一个 DndContext 事件 handler 测试用例, 而非只测下游纯函数。
- [T-FE-010] 多重保护机制只测试了其中一层: saveProtection.test.ts 仅覆盖了 `beforeunload` 事件注册/清理, 未测试 `useBlocker` 路由守卫(任务要求两者)。多项并列保护措施需在测试计划中逐条列出验证用例, 缺一条即产生「测试通过但保护有缺口」的虚假安全感。
- [T-INT-001] Handler 返回 `*model.Xxx` 而非构造的 response struct 导致双向契约漂移: (1) `omitempty` 使零值字段(无预加载的 Entries/User)被静默省略, 前端收到与契约不一致的 JSON; (2) model 中的内部字段(如 EmailVerified)通过 json tag 意外暴露到响应中。根因: 开发时图方便直接序列化 model, 未意识到 model 的 json tag 是为数据库持久化设计的, 与 API 契约是两套不同的序列化需求。预防: 每个 handler 方法必须定义精确匹配契约的 response struct, 逐字段赋值, 禁止将 GORM model 直接传入 `response.Success` / `response.Created`。
- [T-INT-001] Handler 绕开 Service 层直接调用 Repo 破坏四层架构边界: `NewRankingHandler` 同时持有 `rankingService` 和 `rankingRepo` 两个依赖, ListMy/ListPublic 直接调用 `h.rankingRepo.FindByUserID` / `FindPublic` 跳过了 Service 层的 owner 校验和业务逻辑处理。根因: router.go 装配依赖时将 Repo 传给了 Handler 构造函数, 埋下了被绕过 Service 的隐患。预防: router.go 中 Handler 的依赖注入严格只到 Service 层, Repo 仅注入到 Service 构造函数; 新增 handler 时从 router.go 开始确认依赖链不跨层。
- [T-INT-001] goroutine 中无 recover() 导致进程崩溃风险: `GetRanking` 和 `GetRankingByShareCode` 中 `go func() { _ = s.rankingRepo.IncrementView(id) }()` 无 recover 保护, 若 IncrementView 发生 panic 则整个进程崩溃。根因: 简单认为异步加浏览量"不可能出错"而省略了 recover。预防: 所有 `go func()` 必须在一写出来时立即包含 `defer recover()`, 不可信任被调函数一定不会 panic; 能同步调用的异步操作优先同步调用。
- [T-INT-001] 模型 JSON tag 泄露内部字段: `User.EmailVerified` 标记为 `json:"email_verified"` 而非 `json:"-"`, 当 User 通过嵌入关系出现在 `rankingCardResponse` 的 `user` 字段时, 邮箱验证状态意外暴露。根因: 只给明显的敏感字段(Password/VerifyCode)加了 `json:"-"`, 忽略了 EmailVerified/LastLoginAt/CreatedAt/UpdatedAt 等字段在嵌入场景下的静默暴露风险。预防: 新增或修改 Model 字段时, 系统性扫描全部 json tag — 任何契约 schema 中未声明的字段(状态标记、审计字段、系统时间戳)都应标记 `json:"-"`, 不能依赖"这个字段应该不会出现在响应中"的直觉。
- [T-INT-002] macOS 26 + Go 1.22 环境下 dyld LC_UUID 缺失: `go test` 在 macOS 26.5.1 + Go 1.22.12 环境下因二进制 dyld 兼容性问题导致 `signal: illegal instruction` 崩溃。集成测试需要在 `CGO_ENABLED=0` 环境下运行才能通过。新增 Go 测试时如果遇到此类 crash 应优先尝试 `CGO_ENABLED=0 go test`, 这是 macOS 26 平台的兼容性问题, 非项目代码 bug。
- [T-INT-002] 测试辅助函数的 JSON decode error 不可忽略: `json.NewDecoder(resp.Body).Decode(&apiResp)` 忽略 error 会在后端返回非 JSON 响应(如中间件返回纯文本错误)时导致 `apiResp` 为零值, 后续对 `apiResp.Message` / `apiResp.Code` 的断言产生误导性"通过"结果。所有测试辅助函数中 decode/parse 操作的 error 必须被检查并处理(至少 `t.Errorf`), 不可假设"应该不会出错"。
- [T-INT-002] resp.Body.Close() 应在 resp.Result() 后立即 defer: 如果在 Decode 与 Close 之间发生 panic(如非 JSON 响应导致 decode panic), body 会泄漏。标准模式: `resp := w.Result(); defer resp.Body.Close()` 两行紧挨, 中间不夹任何可能 panic 的代码。
- [T-INT-002] 从日志 stdout 解析验证码的方式不可靠: `getVerifyCode` 依赖 `log.SetOutput` 捕获 stdout 并用 `strings.LastIndex` 正则提取验证码, 该方法在日志格式变化、多测试并发共享日志缓冲区、或非测试环境日志框架替换后都会无声失效。验证码类测试应通过在 service 层暴露测试专用查询接口(如 `GetVerifyCode(email)`)或设置固定开发验证码环境变量来解决, 而非解析日志输出。
- [T-INT-002] 循环型测试的注释必须与循环索引逻辑一致: `LoginLockout` 子测试中 0-based 循环执行 5 次(i=0~4), 第 5 次触发锁定, 第 6 次验证锁定。但注释"第 5 次会触发锁定, 第 6 次验证"与实际循环索引表述混淆, 让后续维护者难以判断边界值。写循环型测试时优先用 `for i := 1; i <= N; i++` 的 1-based 循环, 或在注释开头统一标注"i=0 为第 1 次尝试"消除 0-based vs 1-based 的认知偏差。
- [T-INT-002] 空子测试产生虚假覆盖感, 辅助函数必须返回测试所需完整参数: `TestE2E_CompleteFlow/LoginByUsername` 子测试中无任何断言, 仅有一段注释, 在 `go test -v` 中显示为通过, 埋下"用户名登录已经被测试了"的假象。根因: `registerUser` 生成了 `username` 但只返回 `token/email/password/userID` 四个值, 导致后续子测试缺乏所需参数。写子测试时应先写断言骨架再填充逻辑; 若功能尚未实现应使用 `t.Skip("功能未实现")` 而非留空。辅助函数的返回值应覆盖所有调用者可能需要的字段, 否则应提供独立的取值函数。
